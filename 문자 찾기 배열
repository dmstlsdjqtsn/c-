#include<stdio.h>
#pragma warning(disable:4996)
int main()
{
	int i;
	int found=0;
	char a;
	char name[26];
	for (i = 0; i < 26; i++)
		name[i] = 'A' + i;


	scanf("%c", &a);

	for (i = 0; i < 26&&found==0; i++)
		if(name[i]==a)
		found = 1;


	if (found == 1)
		printf("있다");
	else
	printf("없다");
}
